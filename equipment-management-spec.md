# 備品・機材管理アプリ 要件定義書（土台部分）

## 1. プロジェクト概要

### 1.1 目的
本アプリは、SDD（Spec-Driven Development）およびSpec-Kitのデモンストレーションを行うための **土台（ベースアプリ）** である。
本要件定義書に基づき、Claude Codeで実装する。

本アプリ自体は**使い捨て**であり、長期運用・保守性は考慮しない。実装速度とデモのしやすさを優先する。

### 1.2 SDDデモとの関係（スコープの注意）
- 本要件定義書がカバーするのは **土台部分**（ログイン機能＋備品管理CRUD＋検索＋**貸出管理機能**）。
- 貸出管理機能は当初SDDデモ本編での追加を検討したが、5分程度のデモには規模が大きいため、**土台側に含めて事前に実装する**方針に変更した。
- 「CSVエクスポート機能」の追加を、SDDデモ本編で `/specify` `/plan` `/tasks` `/implement` を用いて行うため、**本要件定義書には含めない**（意図的にスコープ外。詳細は7章参照）。
- 土台部分は、SDDのプロセスを踏まずに通常の実装依頼としてClaude Codeに作成させてよい。

### 1.3 想定デモシナリオ（参考情報）
デモ観覧者はJavaに慣れたエンジニアが中心。SDDにより「CSVエクスポート機能」を仕様→計画→タスク分解→実装の流れで追加する様子を見せる（所要時間目安：5分）。土台はその舞台として機能すればよく、凝った作り込みは不要。

---

## 2. 前提条件・制約

| 項目 | 内容 |
|---|---|
| 開発環境 | WSL（Windows Subsystem for Linux）上で開発 |
| コンテナ化 | **Docker不使用**。WSL上に直接ランタイム（JDK等）をインストールして動作させる |
| 起動方法 | ローカル環境でコマンド1つ程度で起動できること（例：`./gradlew bootRun`） |
| ネットワーク | 外部通信なし。すべてローカルで完結する |
| データ永続化 | ファイルベースの組み込みDBを使用し、DBサーバーのインストール・起動を不要にする |
| 想定利用者 | 開発者本人（デモ実施者）のみ。複数人同時アクセスは考慮しない |

---

## 3. 技術スタック

| レイヤー | 技術 |
|---|---|
| 言語 | Java 21（LTS） |
| フレームワーク | Spring Boot 4.1系 |
| ビルドツール | Maven |
| 認証 | Spring Security（フォームログイン） |
| テンプレートエンジン | Thymeleaf（サーバーサイドレンダリング） |
| ORM | Spring Data JPA |
| DB | H2 Database（ファイルモード、組み込み・Docker不要） |
| フロントエンド | **JavaScript不使用**。HTML + `<form>` 送信によるページ遷移のみ |

### 3.1 Spring Initializr 設定

| 項目 | 値 |
|---|---|
| Project | Maven |
| Language | Java |
| Spring Boot | 4.1系 |
| Group | `jp.co.example` |
| Artifact | `equipment-management` |
| Package name | `jp.co.example.equipmentmanagement` |
| Packaging | Jar |
| Java | 21 |

### 3.2 Dependencies（Spring Initializr）

**必須**

| Dependency | 用途 |
|---|---|
| Spring Web | MVCコントローラ、フォーム処理 |
| Thymeleaf | サーバーサイドレンダリングのテンプレートエンジン |
| Spring Data JPA | Equipment・LendingのORM |
| H2 Database | 組み込みDB（ファイルモード） |
| Spring Security | ログイン機能（フォーム認証、ROLE_ADMIN / ROLE_USER） |
| Validation | `@NotBlank`等のバリデーションアノテーション |

**任意（推奨）**

| Dependency | 用途 |
|---|---|
| Spring Boot DevTools | コード変更時の自動リロード |
| Lombok | Entity/DTOの記述量削減 |

**対象外**（本アプリでは選択しない）
- Spring Boot Actuator
- Spring Cloud系
- メッセージング系（Kafka/AMQP等）
- Spring REST Docs等（API化はスコープ外のため）

### 3.3 非機能要件
- インタラクティブなUI（Ajax、SPA的な挙動）は実装しない。画面遷移は通常のHTTPリクエスト（GET/POST）で行う。
- スタイリングは共通CSS（`static/css/app.css`）で統一し、モダンな業務システム風のデザイン（上部ヘッダー＋横並びナビ、カード型レイアウト、状態バッジ）とする。外部CDN・Webフォントは使用しない（ローカル完結）。
- レスポンシブ対応はCSSのみ（メディアクエリ）で行う。スマホ幅（〜768px）ではヘッダーを2段積み、一覧テーブルをカード型表示に切り替え、タブレット幅（〜1024px）ではテーブルを横スクロールで表示する。JavaScriptは使用しない。
- アクセシビリティ対応は不要。

---

## 4. 機能要件

### 4.1 認証機能（ログイン）

#### 概要
Spring Securityによるフォームベースのセッション認証を実装する。

#### ロール
| ロール | 権限 |
|---|---|
| `ROLE_ADMIN`（管理者） | 備品の一覧閲覧・検索、登録・編集・削除がすべて可能 |
| `ROLE_USER`（一般ユーザー） | 備品の一覧閲覧・検索のみ可能（登録・編集・削除は不可、該当ボタン・リンクも非表示） |

#### 初期ユーザー
- ユーザー登録機能は実装しない。アプリ起動時に以下の2ユーザーを初期データとして投入する。

| ユーザー名 | パスワード（平文・デモ用） | ロール |
|---|---|---|
| admin | admin123 | ROLE_ADMIN |
| user | user123 | ROLE_USER |

- パスワードはDB上ではハッシュ化して保存する（BCrypt等）。

#### 画面・挙動
- 未ログイン状態で任意のページにアクセスすると、ログイン画面にリダイレクトする。
- ログイン画面：ユーザー名・パスワードの入力フォームとログインボタン。認証失敗時はエラーメッセージを表示する。
- ログイン後の全画面共通ヘッダーに、ログイン中のユーザー名とログアウトボタンを表示する。
- ログアウト後はログイン画面に戻る。

---

### 4.2 備品管理機能（CRUD）

#### エンティティ：備品（Equipment）

| 項目名 | 型 | 必須 | 備考 |
|---|---|---|---|
| id | 数値（自動採番） | - | 主キー |
| name（品名） | 文字列 | 必須 | 例：「ノートPC」「プロジェクター」 |
| assetNumber（管理番号） | 文字列 | 必須・一意 | 例：「EQ-0001」 |
| location（保管場所） | 文字列 | 任意 | 例：「本社3F倉庫」 |
| status（状態） | 選択式 | 必須 | 「利用可」「貸出中」「故障中」の3値。「貸出中」への変更は4.3節の貸出操作を通じてのみ行われ、編集画面から直接「貸出中」を選択することはできない（貸出中はシステムが自動的に設定・解除する） |
| purchaseDate（購入日） | 日付 | 任意 | |
| createdAt（登録日時） | 日時（自動） | - | |
| updatedAt（更新日時） | 日時（自動） | - | |

#### 画面一覧

| 画面名 | パス（例） | アクセス可能ロール | 内容 |
|---|---|---|---|
| ログイン画面 | `/login` | 全員（未ログイン） | ID/PW入力 |
| 備品一覧画面 | `/equipment` | ADMIN, USER | 一覧表示、検索フォーム、（ADMINのみ）新規登録・編集・削除へのリンク表示 |
| 備品登録画面 | `/equipment/new` | ADMINのみ | 新規登録フォーム |
| 備品編集画面 | `/equipment/{id}/edit` | ADMINのみ | 既存データの編集フォーム |
| 備品詳細画面 | `/equipment/{id}` | ADMIN, USER | 任意（一覧のみで詳細画面を省略してもよい） |
| 貸出履歴画面 | `/lendings` | ADMIN, USER | 貸出・返却履歴の一覧表示（4.3節参照） |

#### 機能詳細

**一覧表示**
- 登録済み備品をテーブル形式で表示する（品名、管理番号、保管場所、状態、購入日）。
- ページネーションは不要（デモ用データは数十件程度を想定）。

**検索・絞り込み**
- 一覧画面上部に検索フォームを配置し、以下の条件で絞り込みができる（GETパラメータ、フォーム送信ベース、JS不使用）。
  - 品名（部分一致）
  - 状態（完全一致、プルダウン選択）

**新規登録**
- ADMINロールのみアクセス可能。品名・管理番号は必須項目としてバリデーションする。
- 管理番号の重複がある場合はエラーメッセージを表示し、登録を拒否する。

**編集**
- ADMINロールのみアクセス可能。登録時と同様のバリデーションを行う。

**削除**
- ADMINロールのみアクセス可能。一覧画面から削除操作を行う（確認ダイアログはブラウザ標準のconfirm等、簡易的なものでよい）。

---

### 4.3 貸出管理機能

#### 概要
備品の貸出・返却を記録する機能。ADMIN・USER双方が貸出・返却の操作を行えるものとする（実運用上、誰でも貸出はする想定のため）。

#### エンティティ：貸出記録（Lending）

| 項目名 | 型 | 必須 | 備考 |
|---|---|---|---|
| id | 数値（自動採番） | - | 主キー |
| equipment | Equipmentへの参照 | 必須 | 貸出対象の備品 |
| employee（借用者） | Employeeへの参照 | 必須 | 社員マスタ（4.4節）から選択する |
| lentAt（貸出日時） | 日時（自動） | - | 貸出登録時に自動設定 |
| dueDate（返却予定日） | 日付 | 任意 | |
| returnedAt（返却日時） | 日時 | - | 未返却の場合はnull |

#### 業務ルール（状態遷移）
- 備品の状態（Equipment.status）が「利用可」の場合のみ、貸出登録が可能。
- 「貸出中」または「故障中」の備品に対しては、貸出登録操作自体を行えないようにする（一覧画面上でボタンを非表示、もしくは操作不可メッセージを表示）。
- 貸出登録を行うと、対象備品の状態は自動的に「貸出中」に変更される。
- 返却操作を行うと、対象の貸出記録に`returnedAt`が設定され、対象備品の状態は自動的に「利用可」に戻る。
- 1つの備品に対して同時に有効な貸出記録（`returnedAt`がnull）は1件のみ存在できる。

#### 画面・操作
- **備品一覧画面**：各行に、状態に応じて以下のいずれかを表示する。
  - 状態が「利用可」の備品 → 「貸出する」ボタン（クリックで借用者選択フォームへ遷移、またはインラインフォームで社員マスタから借用者（employeeId）を選択し、dueDateを入力しPOST）
  - 状態が「貸出中」の備品 → 「返却する」ボタン（現在の借用者名を表示し、クリックで返却処理を実行）
  - 状態が「故障中」の備品 → 操作ボタンなし
- **貸出履歴画面（`/lendings`）**：全ての貸出記録（返却済み・未返却含む）を一覧表示する。備品名・借用者名・貸出日時・返却予定日・返却日時（未返却の場合は「未返却」と表示）をテーブルで表示する。検索・絞り込みは不要（一覧表示のみでよい）。

---

### 4.4 社員管理機能（社員マスタ）

#### 概要
貸出記録の借用者を管理するための社員マスタ。以前は貸出記録の借用者名を自由入力としていたが、社員をマスタで管理したいという要望に伴い、貸出時は社員マスタから選択する方式に変更した。

#### エンティティ：社員（Employee）

| 項目名 | 型 | 必須 | 備考 |
|---|---|---|---|
| id | 数値（自動採番） | - | 主キー |
| employeeNumber（社員番号） | 文字列 | 必須・一意 | 例：「E-001」 |
| name（氏名） | 文字列 | 必須 | |
| department（部署） | 文字列 | 任意 | |
| createdAt（登録日時） | 日時（自動） | - | |
| updatedAt（更新日時） | 日時（自動） | - | |

#### 画面一覧

| 画面名 | パス（例） | アクセス可能ロール | 内容 |
|---|---|---|---|
| 社員一覧画面 | `/employees` | ADMIN, USER | 一覧表示、（ADMINのみ）新規登録・編集・削除へのリンク表示 |
| 社員登録画面 | `/employees/new` | ADMINのみ | 新規登録フォーム |
| 社員編集画面 | `/employees/{id}/edit` | ADMINのみ | 既存データの編集フォーム |

#### 機能詳細
- **一覧表示**：登録済み社員をテーブル形式で表示する（社員番号、氏名、部署）。検索・絞り込みは不要。
- **新規登録・編集**：ADMINロールのみアクセス可能。社員番号・氏名は必須項目としてバリデーションし、社員番号の重複がある場合はエラーメッセージを表示し登録・更新を拒否する。
- **削除**：ADMINロールのみアクセス可能。貸出履歴（Lending）が存在する社員は、参照整合性のため削除できない（4.2節の備品削除と同様の考え方）。

---

## 5. データ投入（初期データ）

アプリ起動時に以下を自動投入する（`data.sql`やCommandLineRunner等、実装方法はClaude Codeの判断に委ねる）。

- ユーザー：4.1節に記載の2ユーザー（admin, user）
- 社員：貸出記録の借用者として参照できるよう、サンプル社員データを数件投入する
- 備品：デモ画面が寂しくならない程度のサンプルデータを5〜10件程度、状態（利用可／貸出中／故障中）が混在する形で投入する
- 貸出記録：状態が「貸出中」の備品に対応する貸出記録（未返却）を1〜2件、加えて返却済みの履歴データを1〜2件投入する（借用者は投入済みの社員マスタから参照する）

---

## 6. 環境構築・起動（想定）

- WSL上でJDK 21をインストール
- プロジェクトルートで以下のようなコマンドにより起動できること
  ```
  ./mvnw spring-boot:run
  ```
- ブラウザで `http://localhost:8080` にアクセスするとログイン画面が表示される
- DBファイル（H2）はプロジェクト内に生成され、Docker等の追加インストールは不要

---

## 7. スコープ外事項（本要件定義書では扱わない）

- **CSVエクスポート機能** ※SDDデモ本編（所要時間目安5分）で別途仕様化し、`/specify` `/plan` `/tasks` `/implement` の流れで追加する。本要件定義書には含めない。
- ユーザー登録・パスワード変更・権限管理画面
- 監査ログ・操作履歴（貸出記録自体は4.3節の機能として含むが、それ以外の操作履歴は対象外）
- 貸出の延滞通知・アラート機能
- API化（REST API等）・外部システム連携
- 複数ユーザーによる同時アクセス・排他制御
- アクセシビリティ対応（WAI-ARIA、スクリーンリーダー対応等）
